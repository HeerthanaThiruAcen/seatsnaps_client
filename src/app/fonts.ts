import { Poppins, DM_Sans } from "next/font/google";

export const poppins = Poppins({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const dmSans = DM_Sans({
  subsets: ["latin"],
  display: "swap",
});
